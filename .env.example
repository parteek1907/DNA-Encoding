# Optional: Provide a PostgreSQL connection string for persistent storage.
# If omitted, the app automatically uses in-memory storage.
DATABASE_URL=postgresql://user:password@localhost:5432/dna_encoding
